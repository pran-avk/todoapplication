import 'package:flutter/material.dart';

AppBar customAppbar(){
  return AppBar(
    title: Text('django Todos'),
    centerTitle: true,
    backgroundColor: Colors.black12,
    elevation: 0.8,
  );
}